# ORI Routine Run — 2026-06-20

**Status:** FAILED — data not found

## What was checked
- `audaddy/ori-app/data/` — directory does not exist
- No JSON or CSV biometric files found anywhere in the repository

## Blockers
1. **No data pipeline output**: The `/data/` directory has never been created. The ORI app (index.html) runs client-side and does not write back to the repo. Biometric data from Garmin/Oura needs to be exported and committed to `/data/` for this routine to function.
2. **sherpa6-vault not in session scope**: The write-back target `audaddy/sherpa6-vault` is not accessible from this session. The session is scoped to `audaddy/ori-app` only.

## Action required
- Add `audaddy/sherpa6-vault` to the session's allowed repositories, OR configure the routine to write to `audaddy/ori-app` instead.
- Set up a data export pipeline that writes Oura/Garmin data as JSON/CSV to `data/` in this repo before the routine runs.

## Brief output
```
ORI data not found — manual log required.
```
